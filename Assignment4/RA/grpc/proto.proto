syntax = "proto3";
package proto;
option go_package = "./pb";

message request{
    string portnr = 1;
    int32 lamport = 2;
}

message response {
    string portnr = 1;
    int32 lamport = 2;
}

message empty {}

service RA {
    rpc RequestAccess (request) returns (empty){};
    rpc SendPermission (response) returns (empty) {};
}